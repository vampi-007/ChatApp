import { Form, FormControl, FormGroup } from "react-bootstrap";

function SendMessage(){

    return(
        <Form>
            <FormGroup>
                <FormControl type="text"/>
            </FormGroup>
        </Form>
    )

}